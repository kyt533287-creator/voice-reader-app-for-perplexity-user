# 引き継ぎメモ（2026-03-12）

## このスレッドで完成したこと

### 機能追加
| 機能 | ファイル | 概要 |
|---|---|---|
| インタースティシャル広告 | MainActivity.kt / build.gradle.kts | 再生終了3回に1回。BuildConfig経由でID管理 |
| オーディオフォーカス管理 | TtsService.kt | 電話着信で自動停止・終話後自動再開。他アプリは停止のみ |
| シーク可能プログレスバー | MainActivity.kt / TtsService.kt | ドラッグで任意位置にジャンプ。TtsService.seekTo()を追加 |
| プログレスバー常時表示 | MainActivity.kt | sheetPeekHeight 205→260dpに拡大 |

### バグ修正
| バグ | 修正内容 |
|---|---|
| 別アプリ起動時にセンテンスを最後まで読む | onDone()先頭に `if (!isCurrentlyPlaying) return` を追加 |
| BuildConfig 71件エラー | `com.tom_roush.pdfbox.BuildConfig` の誤importを削除 |
| ダイアログボタン順バラバラ | 全ダイアログをYES左・NO右に統一 |
| ハイライトがボトムシートに隠れる | スクロールオフセットを `visibleCount/2 - 2` → `visibleCount/2 + 2` に修正 |

### リネーム
- アプリ名：VoiceReader → **BridgeTTS**
- strings.xml / AndroidManifest / TtsService通知タイトルを変更
- パッケージ名 `com.example.voicereader` は変更なし

### AdMob
- 審査承認済み ✅
- 本番IDをlocal.propertiesに設定済み

## 次のスレッドでやること（優先順）

1. **アプリアイコン作成**
   - デスクトップに `bridgettsicon.html` あり（Chromeで開いてDevTools→Capture node screenshot）
   - Android Studio Image Asset Studioで全サイズ自動生成

2. **Keystoreの生成**
   - Build → Generate Signed Bundle / APK
   - パスワードとファイルは絶対に失くさない（再アップロードに必要）

3. **署名付きAABのビルド**

4. **プライバシーポリシー作成**
   - 広告入りアプリはGoogle Playで必須
   - 無料テンプレートサービスで作成→GitHub Pagesなどで公開

5. **Play Console申請**
   - $25のGoogle Play Developer アカウントが必要

## 現在のブランチ状態
- ブランチ: `feat/ui-improvements-portrait-lock`
- リモートより3コミット先行（未push）
- 未コミットの変更：今回のUI調整（seekスライダー・ハイライト位置・コントラスト等）
